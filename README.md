# Q-Learning Lab

An interactive Q-Learning practice application where you can configure the environment, move the agent, and manually calculate Q-values.

## 🏃 Run Locally

**Prerequisites:**  Node.js (v18 or higher recommended)

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run the development server:
   ```bash
   npm run dev
   ```

3. Open [http://localhost:3000/Q-Learning-Lab/](http://localhost:3000/Q-Learning-Lab/) in your browser

## 🛠️ Build for Production

To build the app for production:

```bash
npm run build
```

To preview the production build locally:

```bash
npm run preview
```
